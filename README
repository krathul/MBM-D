Starting with dynamic updates
 commands currently used for bulding:
 mkdir build
 cd build/
 cmake ../
 make

Command to run
  ./MBM [initial_graph] [number_of_vertices] [update_graph] [optional_arg:thread numbers]
  
